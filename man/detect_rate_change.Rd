% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifacts.R
\name{detect_rate_change}
\alias{detect_rate_change}
\title{Detect rate-of-change anomalies in a wearable time series}
\usage{
detect_rate_change(
  x,
  window = "15 min",
  threshold = 3,
  scale = c("mad", "sd", "none")
)
}
\arguments{
\item{x}{A \code{wa_ts} tibble.}

\item{window}{Character string describing the maximum separation between
points used to compute rate-of-change.}

\item{threshold}{Numeric threshold applied to the scaled rate-of-change.}

\item{scale}{Scaling strategy: MAD, standard deviation, or "none".}
}
\value{
A tibble like \code{\link[=detect_flatlines]{detect_flatlines()}} with \code{type} values \code{"roc_up"} or
\code{"roc_down"} and a \code{strength} column containing the maximum scaled
deviation within each segment.
}
\description{
Detect rate-of-change anomalies in a wearable time series
}
\examples{
data <- toy_cgm(n_id = 1, n = 12)
ts <- as_wearable_ts(data, id = id, time = time, value = value)
ts$value[6] <- ts$value[5] + 60
detect_rate_change(ts, window = "15 min", threshold = 3)
}
