% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifacts.R
\name{detect_saturation}
\alias{detect_saturation}
\title{Detect saturation segments in a wearable time series}
\usage{
detect_saturation(
  x,
  lower = attr(x, "lower"),
  upper = attr(x, "upper"),
  min_len = "10 min"
)
}
\arguments{
\item{x}{A \code{wa_ts} tibble.}

\item{lower, upper}{Numeric bounds defining saturation thresholds. Defaults to
the metadata carried by the \code{wa_ts} object.}

\item{min_len}{Minimum duration (character string) that a saturation run must
span to be reported.}
}
\value{
A tibble structured like \code{\link[=detect_flatlines]{detect_flatlines()}} with type values
\code{"saturation_high"} or \code{"saturation_low"}.
}
\description{
Detect saturation segments in a wearable time series
}
\examples{
data <- toy_cgm(n_id = 1, n = 12)
ts <- as_wearable_ts(data, id = id, time = time, value = value)
ts$value[5:9] <- 405
detect_saturation(ts, min_len = "10 min")
}
