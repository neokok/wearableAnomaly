% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{as_wearable_ts}
\alias{as_wearable_ts}
\title{Coerce data into a wearable-anomaly time series tibble}
\usage{
as_wearable_ts(
  data,
  id,
  time,
  value,
  tz = "UTC",
  lower = 40,
  upper = 400,
  cadence = NULL
)
}
\arguments{
\item{data}{A data frame containing subject identifiers, timestamps, and
signal values.}

\item{id, time, value}{Tidy-select expressions identifying the identifier,
timestamp, and value columns respectively.}

\item{tz}{Time zone used when coercing the \code{time} column to POSIXct. Defaults
to "UTC".}

\item{lower, upper}{Numeric bounds used when assessing valid signal values.}

\item{cadence}{Optional numeric cadence (in seconds) describing the expected
sampling interval. Stored as metadata when supplied.}
}
\value{
A tibble with class \code{wa_ts} and attributes \code{lower}, \code{upper},
\code{cadence} (stored as \code{NA} when not supplied), \code{tz}, and \code{dups_dropped}.
}
\description{
Coerce data into a wearable-anomaly time series tibble
}
\examples{
data <- toy_cgm(n_id = 2, n = 6)
wa <- as_wearable_ts(data, id = id, time = time, value = value)
validate_ts(wa)
}
