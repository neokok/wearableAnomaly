# Placeholder images for GenAI tutorial

Store screenshots referenced in `docs/articles/genai-tutorial.md` inside this directory. Pkgdown keeps files under `man/figures/` when rendering vignettes, so adding this placeholder ensures the path exists in version control.
