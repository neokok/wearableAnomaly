% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_ts}
\alias{validate_ts}
\alias{has_issues}
\title{Validate a wearable-anomaly time series object}
\usage{
validate_ts(x, lower = attr(x, "lower"), upper = attr(x, "upper"))

has_issues(issues)
}
\arguments{
\item{x}{A \code{wa_ts} object created by \code{\link[=as_wearable_ts]{as_wearable_ts()}}.}

\item{lower, upper}{Numeric bounds used to flag out-of-range values. Defaults
to the attributes stored on \code{x}.}
}
\value{
A tibble recording detected issues with columns \code{issue}, \code{id},
\code{time}, \code{value}, and \code{details}. A zero-row tibble is returned when no
issues are found.
}
\description{
Validate a wearable-anomaly time series object
}
\examples{
data <- toy_cgm(n_id = 1, n = 6)
ts <- as_wearable_ts(data, id = id, time = time, value = value)
validate_ts(ts)
}
