% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifacts.R
\name{detect_flatlines}
\alias{detect_flatlines}
\title{Detect flatline segments in a wearable time series}
\usage{
detect_flatlines(x, tol = 1e-06, min_len = "15 min")
}
\arguments{
\item{x}{A \code{wa_ts} tibble.}

\item{tol}{Numeric tolerance used when comparing consecutive signal values.}

\item{min_len}{Minimum duration (character string understood by
\code{\link[base:difftime]{base::as.difftime()}}) that a flat segment must span to be reported.}
}
\value{
A tibble with columns \code{id}, \code{start}, \code{end}, \code{duration}, \code{type},
\code{value}, and \code{strength}. A zero-row tibble is returned when no flatlines are
detected.
}
\description{
Detect flatline segments in a wearable time series
}
\examples{
data <- toy_cgm(n_id = 1, n = 12)
ts <- as_wearable_ts(data, id = id, time = time, value = value)
ts$value[4:8] <- 125
detect_flatlines(ts, tol = 1e-5, min_len = "15 min")
}
