% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pelt.R
\name{detect_changepoints_pelt}
\alias{detect_changepoints_pelt}
\title{Detect changepoints in wearable signals using the PELT algorithm}
\usage{
detect_changepoints_pelt(
  x,
  cost = c("meanvar", "mean", "poisson"),
  penalty = c("MBIC", "BIC", "AIC"),
  min_seg_len = 12L
)
}
\arguments{
\item{x}{A \code{wa_ts} tibble produced by \code{\link[=as_wearable_ts]{as_wearable_ts()}}.}

\item{cost}{Cost function to optimise. Choose from \code{"meanvar"} (default),
\code{"mean"}, or \code{"poisson"}.}

\item{penalty}{Penalty strategy. Options are \code{"MBIC"}, \code{"BIC"}, and
\code{"AIC"}.}

\item{min_seg_len}{Minimum number of observations permitted in any segment.}
}
\value{
A tibble with the detected changepoints containing columns \code{id},
\code{cp_time}, \code{cp_index}, \code{new_level}, \code{new_var}, \code{method}, and \code{penalty}.
Returns a zero-row tibble with these columns when no changepoints are
detected.
}
\description{
Detect changepoints in wearable signals using the PELT algorithm
}
\examples{
set.seed(123)
n <- 240
times <- seq.POSIXt(as.POSIXct("2025-01-01 00:00:00", tz = "UTC"),
                    by = "5 min", length.out = n)
values <- c(rnorm(120, mean = 100, sd = 5), rnorm(120, mean = 130, sd = 5))
data <- dplyr::tibble(id = "id01", time = times, value = values)
ts <- as_wearable_ts(data, id = id, time = time, value = value)
detect_changepoints_pelt(ts, cost = "meanvar", penalty = "MBIC")
}
